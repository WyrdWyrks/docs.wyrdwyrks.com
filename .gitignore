_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/
Gemfile.lock

# Obsidian: ignore the churny per-machine pane layout, but keep the rest of
# .obsidian/ committed so vault settings travel with the repo.
.obsidian/workspace.json
.obsidian/workspace-mobile.json
